BUSYBOX=busybox-1.19.3
BUSYBOX_SRC=$DIST_DIR/src/$BUSYBOX.tar.bz2
BUSYBOX_CONFIG=$(dirname $BUSYBOX_BUILD_CFG)/$BUSYBOX.config

DROPBEAR=dropbear-0.53.1
DROPBEAR_SRC=$DIST_DIR/src/$DROPBEAR.tar.bz2
DROPBEAR_CONFIGURE_OPTS="--host=$ARCH-linux"

KEXEC_TOOLS=kexec-tools-2.0.2
KEXEC_TOOLS_SRC=$DIST_DIR/src/$KEXEC_TOOLS.tar.gz
KEXEC_TOOLS_CONFIGURE_OPTS="--host=$ARCH-linux"
